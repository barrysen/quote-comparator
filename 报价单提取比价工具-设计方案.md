# 报价单提取比价工具 · 设计方案

> 版本：v1.0 ｜ 用途：作为开发任务书，可直接交给开发 Agent 执行
> 阅读对象：开发 Agent / 开发者本人
> 前置说明：开发者暂无真实数据，本项目以**自编的虚拟样本集**驱动开发与验收（见 §9）

---

## 1. 项目概述

### 1.1 要解决的问题

采购人员向多家供应商询价同一批物料后，会收到格式五花八门的报价回复：Excel 表、PDF 报价单、邮件正文、微信消息文字、手机拍照图片。人工把这些报价誊抄到一张表里横向比价，耗时且易错。

本工具将 **多种格式、多家供应商的报价文件自动提取为统一的结构化数据，生成一张横向比价 Excel**，并对异常报价（偏离均价、交期过长、缺项漏报）自动标色预警。

### 1.2 目标与非目标

**目标（v1.0 必须做到）：**
- 支持输入格式：Excel（.xlsx/.xls）、PDF（电子版 + 扫描/拍照版）、纯文本（邮件/微信复制内容，.txt/.md/.eml 导出文本）、图片（.jpg/.png）
- 每个文件视为"一家供应商的一次报价"，一个文件可含多条物料报价行
- 提取字段：物料名称、规格型号、数量、单价、金额、单位、交期、付款方式、备注
- 按物料对齐多家报价，输出比价总表
- 异常自动标色 + 低置信度字段标黄，供人工复核
- 批量处理一个文件夹；纯本地运行，LLM 端点可配置（DeepSeek API / 本地模型）

**非目标（v1.0 明确不做，防止范围蔓延）：**
- 不做供应商主数据管理、不做历史价格数据库（v2.0）
- 不做自动发询价邮件、不接 ERP/SRM 系统
- 不做税率/汇率/运费的总价精算（仅按报价原文提取，最多做数量×单价≈金额的合理性检查）
- 不做 Web 平台，v1.0 为命令行工具
- 不做合同、订单等其他单据类型（仅报价单）

### 1.3 使用场景

```
用户（采购工程师）：
  把"XX 项目询价"文件夹（含 5 家供应商的 7 个报价文件）拖给工具
    → 工具逐个识别供应商并提取报价明细
    → 生成比价 Excel：每行一个物料，每列一家供应商
    → 最高价标红、最低价标绿、缺报标灰、低置信度标黄
    → 用户只复核标黄部分，其余直接用
```

---

## 2. 总体架构

### 2.1 处理流水线

与"图纸提取"同一套架构思想：**确定性流水线，LLM 只负责脏数据清洗，能规则解决的不用 LLM**。

```
┌─────────┐   ┌──────────┐   ┌────────────┐   ┌─────────────┐
│ 1. 分拣  │ → │ 2. 原始   │ → │ 3. LLM      │ → │ 4. 规则      │
│ 格式识别 │   │ 内容提取  │   │ 结构化解析  │   │ 校验        │
└─────────┘   └──────────┘   └────────────┘   └─────────────┘
                                                      ↓
┌─────────┐   ┌──────────┐   ┌────────────┐
│ 6. 导出  │ ← │ 5. 比价   │ ← │ 统一物料    │
│ Excel   │   │ 分析标色  │   │ 报价行集合  │
└─────────┘   └──────────┘   └────────────┘
```

**与图纸方案的关键差异**：报价单没有固定的"明细栏区域"，格式自由度极高（有的根本没有表格，就是一段话）。因此**本工具不做区域定位**，改为"全文提取 → LLM 理解"路线——LLM 在本项目中承担的角色比图纸方案更重，这是报价单场景决定的，但防幻觉措施也相应加强（见 §3.3、§3.4）。

### 2.2 技术选型

| 组件 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.11+ | 与图纸方案同栈，未来可合并代码库 |
| Excel 读取 | `openpyxl`（.xlsx）+ `xlrd`（.xls） | 标准选择 |
| 电子 PDF | `pdfplumber` | 文本+表格线提取 |
| 扫描/图片 OCR | `PaddleOCR`（本地） | 中文好、离线；备选通道：视觉大模型 |
| 图片预处理 | `OpenCV` | 拍照件的旋转/透视矫正 |
| LLM 调用 | OpenAI 兼容接口（`openai` SDK） | 端点可换：DeepSeek API / 本地 vLLM |
| 结构化输出 | JSON Schema 约束 + `pydantic` 校验 | 保证可解析 |
| Excel 导出 | `openpyxl` | 需要单元格标色 |
| 配置 | YAML | 非程序员可编辑 |
| 测试 | `pytest` + 自编虚拟样本集 | 见 §9 |

---

## 3. 各阶段详细设计

### 3.1 阶段一：格式分拣

**输入**：文件路径
**输出**：`{"file": "...", "kind": "excel|pdf_digital|pdf_scanned|text|image"}`

**逻辑**：
1. 按扩展名初分：`.xlsx/.xls` → excel；`.txt/.md/.eml` → text；`.jpg/.jpeg/.png` → image；`.pdf` → 需二次判定
2. PDF 二次判定：`pdfplumber` 读取首页，可提取字符数 > 100 → `pdf_digital`，否则 → `pdf_scanned`
3. 不支持的格式记入错误清单，不中断批处理

### 3.2 阶段二：原始内容提取（统一为"文本+结构提示"）

目标：把任何格式的文件转成**保留结构信息的文本**，交给 LLM。不做业务理解。

| 输入类型 | 提取方式 |
|---|---|
| excel | 逐 sheet 读取，转成 Markdown 表格文本（保留单元格原值，空单元格留空）。多 sheet 全部输出，注明 sheet 名 |
| pdf_digital | `pdfplumber` 提取全文文本 + `extract_table()` 提取到的表格（Markdown 化），两者都保留 |
| pdf_scanned | 转 300 DPI 图片 → OpenCV 矫正 → PaddleOCR 全文识别（保留行结构）；OCR 置信度低于阈值的行记录 `low_ocr_lines` |
| image | 同 pdf_scanned 的 OCR 管线 |
| text | 直接读取（`chardet` 处理编码） |

**输出中间产物（落盘 JSON）：**
```json
{
  "source_file": "供应商A报价.xlsx",
  "kind": "excel",
  "raw_text": "Sheet: 报价单\n| 物料 | 规格 | 数量 | 单价 | ...",
  "extraction_warnings": []
}
```

### 3.3 阶段三：LLM 结构化解析（核心环节）

**输入**：阶段二的 `raw_text`
**输出**：符合 Schema 的 `SupplierQuote`（见 §4）

**实现要求：**
1. OpenAI 兼容接口 + `response_format` JSON Schema 强制结构化输出；temperature=0；超时 60s；失败指数退避重试 3 次
2. Prompt（存于 `config/prompts/parse_quote.md`）包含：
   - 任务说明：从报价文本中提取供应商名称和逐行报价明细
   - 5 组 few-shot 示例：覆盖 Excel 表格型、PDF 正式报价单型、邮件散文化、微信简讯型、OCR 噪音型
   - **硬性禁令**：所有字段值必须逐字来自原文；原文没有的字段一律留空并把该行 `confidence` 标为 `low`；**绝不允许推算、补全、四舍五入**（数量和单价原文怎么写就怎么提取，计算交给规则阶段）
   - 每行输出 `confidence: high|low` + `source_snippet`（该行数据在原文中的原文片段，必须能在原文中检索到）
   - 供应商名称识别优先级：文件内容中的公司全称 > 文件名 > "未知供应商"
3. **溯源硬校验**（代码实现，不靠 LLM 自觉）：解析返回后，代码检查每个 `source_snippet` 是否为 `raw_text` 的子串（归一化空白后）。不是子串的，该行强制 `confidence=low` 并加 flag `H1 幻觉嫌疑`
4. LLM 连续重试失败：该文件降级为"原始文本原样导出到单独 Sheet + 整体标黄"，记入失败清单，不中断批处理
5. 大文件处理：`raw_text` 超过模型上下文 80% 时按表格行/段落切块分批提取后合并（报价单极少超长，简单切块即可，不做语义切分）

### 3.4 阶段四：规则校验

每条规则一个独立函数 `check(quote: SupplierQuote) -> list[Finding]`，全部可单测、不依赖 LLM：

| 规则 | 检查内容 | 处理 |
|---|---|---|
| V1 必填 | 物料名称、单价为空 | 标黄 |
| V2 数值解析 | 数量/单价文本解析为数值（处理"4件""¥1,200.00""含税13%"等） | 自动解析；解析失败标黄，保留原文 |
| V3 金额核对 | 若原文同时有数量、单价、金额：检查 数量×单价 ≈ 金额（±2%容差） | 不符标黄并注明差额（常见于报价笔误，是真实痛点） |
| V4 币种/税率 | 识别币种（默认 CNY）与"含税/未税"字样 | 标注到字段，不做换算 |
| V5 交期解析 | 交期文本解析为天数字段（"7天""两周""现货"→0） | 解析失败保留原文并标黄 |
| V6 单位归一 | 单位映射到标准表（个/件/pcs/PCS → 件） | 自动归一，映射表在 `config/units.yml` |
| V7 供应商一致性 | 同文件出现多个供应商名 | 标黄整个文件 |

### 3.5 阶段五：比价分析

跨文件对齐物料，产出比价矩阵。**物料对齐是本阶段核心难点**——同一物料在不同报价单里名称写法不同（"内六角螺钉 M8×20" vs "M8*20 内六角螺丝"）：

1. **第一遍（规则）**：归一化（去空格、统一×/*、转小写、全半角统一）后按 "名称+规格" 精确匹配分组
2. **第二遍（LLM 兜底）**：对未匹配的孤立行，把候选清单给 LLM 做"是否为同一物料"的判定（仍是结构化输出 + 每对输出置信度）；匹配置信度 low 的一律不合并、在 Excel 中相邻放置并标黄提示人工确认
3. 每个物料组计算：报价家数、最低/最高价、均价、各报价偏离均价百分比、最短/最长交期
4. 缺报检测：某物料有 ≥2 家报价但某供应商未报 → 该供应商该物料标"缺报"

**比价标色规则（写入 Excel）：**
| 情形 | 颜色 |
|---|---|
| 该物料最低价 | 绿（`C6EFCE`） |
| 高于均价 15% 以上 | 红（`FFC7CE`） |
| 缺报 | 灰（`D9D9D9`） |
| 任何低置信度/校验未过 | 黄（`FFF3CD`），黄色优先级最高，覆盖其他颜色 |
| 交期为该物料最长且超过中位数 2 倍 | 交期单元格标橙（`FFE0B2`） |

### 3.6 阶段六：Excel 导出

**输出工作簿结构（openpyxl）：**
- **Sheet "比价总表"**：行=物料，列=供应商。每个供应商占一组列（单价、数量、交期、置信度）。含均价/最低价/偏离度辅助列。冻结首行首列、加筛选
- **Sheet "明细数据"**：长表，一行一条报价记录，含全部提取字段 + 来源文件 + 原文片段（`source_snippet`，供人工对照溯源）
- **Sheet "统计与异常"**：文件处理统计、失败清单、缺报清单、V3 金额不符清单
- 同时输出 `result.json`（完整结构化结果）和 `report.txt`（人读摘要）

---

## 4. 数据模型（pydantic）

```python
class QuoteLine(BaseModel):
    item_name: str              # 物料名称（必填，空则 V1 标黄）
    spec: str = ""              # 规格型号
    quantity_raw: str = ""      # 数量原文
    quantity: float | None      # 解析后数量
    unit: str = ""              # 归一化单位
    unit_price_raw: str = ""    # 单价原文（必填）
    unit_price: float | None    # 解析后单价
    amount_raw: str = ""        # 金额原文（如有）
    currency: str = "CNY"
    tax_included: bool | None   # 含税与否（原文明确才有值）
    lead_time_raw: str = ""     # 交期原文
    lead_time_days: int | None  # 解析后交期天数
    payment_terms: str = ""     # 付款方式
    remark: str = ""
    confidence: Literal["high", "low"]
    source_snippet: str         # 原文片段（H1 溯源校验的对象）
    flags: list[str] = []       # 触发的规则号，如 ["V3","H1"]

class SupplierQuote(BaseModel):
    source_file: str
    kind: str
    supplier_name: str          # 识别不到则 "未知供应商"
    lines: list[QuoteLine]
    warnings: list[str] = []
```

---

## 5. 项目结构

```
quote-comparator/
├── README.md
├── pyproject.toml
├── config/
│   ├── settings.yml          # LLM 端点/模型、OCR 阈值、比价参数(偏离阈值15%等)
│   ├── units.yml             # 单位归一映射表
│   └── prompts/
│       ├── parse_quote.md    # 阶段三 prompt（含 few-shot）
│       └── match_items.md    # 阶段五物料对齐 prompt
├── src/
│   ├── cli.py                # extract <文件夹> -o 比价结果.xlsx [--force]
│   ├── pipeline/
│   │   ├── classify.py       # 阶段一
│   │   ├── extract_raw.py    # 阶段二
│   │   ├── parse_quote.py    # 阶段三（含 H1 溯源硬校验）
│   │   ├── validate.py       # 阶段四 V1~V7
│   │   ├── compare.py        # 阶段五 比价分析
│   │   └── export.py         # 阶段六
│   ├── llm/
│   │   └── client.py         # 统一的 LLM 客户端：重试/超时/JSON Schema/分块
│   └── models.py
├── intermediates/            # 中间产物缓存（断点续跑）
├── tests/
│   ├── fixtures/             # 虚拟样本集（见 §9）
│   │   └── expected/         # 每份样本对应 expected.json
│   ├── test_classify.py
│   ├── test_extract_raw.py
│   ├── test_validate.py      # V1~V7 逐条单测（不依赖 LLM）
│   ├── test_compare.py       # 比价/标色逻辑单测（手工构造 QuoteLine）
│   └── test_e2e.py           # 虚拟样本端到端回归
└── output/
```

---

## 6. 接口契约（开发 Agent 必须遵守）

1. 每个 `pipeline/*.py` 暴露纯函数 `run(input, config) -> output`，模块间禁止共享全局状态
2. 每阶段输出写入 `intermediates/{文件名}/{阶段}.json`，存在则跳过（`--force` 重跑）
3. LLM 只允许经 `llm/client.py` 调用，其余模块禁止 import openai
4. 校验规则函数签名统一：`check(...) -> list[Finding]`，`Finding = (对象定位, 规则号, 说明)`
5. 所有金额/数量的**计算只发生在规则与比价阶段**，LLM 输出层禁止出现计算值
6. CLI：`python -m src.cli extract ./报价文件夹 -o 比价结果.xlsx --config config/settings.yml [--force]`

---

## 7. 验收标准（Definition of Done）

**功能验收（全部基于虚拟样本集）：**
- [ ] 5 类格式（Excel / 电子 PDF / 扫描 PDF / 文本 / 图片）样本全部端到端跑通
- [ ] **关键字段（物料名、单价、数量）提取准确率 ≥ 95%**；错误字段必须被标黄捕获（"错且未标黄"才算事故）
- [ ] H1 溯源校验有效：人为构造 LLM 返回含原文不存在片段的 mock 响应，对应行必须被强制标黄
- [ ] 比价标色正确：测试集中预设的最低价/缺报/超均价 15% 情形全部正确标色
- [ ] 单文件损坏/LLM 持续失败不中断批处理，失败清单出现在报告中

**工程验收：**
- [ ] V1~V7 每条规则独立单测，不依赖网络
- [ ] LLM 客户端有 mock 重放测试
- [ ] `settings.yml` 一键切换 LLM 端点
- [ ] README：安装、配置、使用、如何新增样本

**准确率口径**（写进 test_e2e.py）：
```
字段准确率 = 1 - (值错误且未标黄的关键字段数 / 关键字段总数)
```
核心承诺：**允许错，但不允许错了不告诉你。**

---

## 8. 分阶段开发计划

| 里程碑 | 内容 | 完成标志 |
|---|---|---|
| M1 骨架 | 项目结构 + CLI + 配置 + 数据模型 + 中间产物机制 | 空文件夹跑通、生成空报告 |
| M2 Excel+文本主线 | 阶段一、二（excel/text）、三、四（V1/V2/V6）+ 明细导出 | 3 份 Excel/文本虚拟样本提取正确 |
| M3 比价引擎 | 阶段五（规则对齐 + LLM 兜底）+ 比价总表标色 + 统计 Sheet | 预设比价情形全部标色正确 |
| M4 PDF 与 OCR | pdf_digital / pdf_scanned / image 管线 | 扫描件样本关键字段准确率达标 |
| M5 打磨 | V3/V4/V5/V7 补全、report.txt、性能、README | 通过全部验收标准 |

**执行要求：**
- 每个里程碑跑完全部测试再进入下一个
- 遇到 §1.2 非目标需求，记入 `BACKLOG.md`，不实现
- 物料对齐的 LLM 兜底默认保守：**宁可不合并标黄提示，不可错误合并**

---

## 9. 虚拟样本集（关键：没有真实数据时的开发燃料）

开发 Agent 必须先**生成样本再开发**。在 `tests/fixtures/` 下生成一个虚拟询价项目："XX 产线备件采购"，5 家虚拟供应商（华东机电、恒力工贸、鑫源自动化、铭泰五交化、广联轴承），同一批 8 种物料（如：内六角螺钉 M8×20、6204 深沟球轴承、304 不锈钢球阀 DN15、工业吸尘器滤芯……）。

每家供应商用**不同格式**回报价，且**刻意埋入脏数据**：

| 文件 | 格式 | 埋入的问题 |
|---|---|---|
| 华东机电.xlsx | 规范表格 | 无（基准样本）；其中 1 行金额≠数量×单价（验 V3） |
| 恒力工贸.pdf | 电子 PDF 正式报价单 | 含税/未税混写；1 种物料缺报（验缺报） |
| 鑫源自动化.pdf | 扫描 PDF（生成后转图片再合成 PDF） | OCR 噪音："0/O""1/I"混淆 |
| 铭泰五交化.txt | 微信消息风格文本 | 无表格的散文报价；"两周"式模糊交期；单位"个/pcs"混用 |
| 广联轴承.png | 拍照图片（表格图加透视畸变+轻微模糊生成） | 图像质量差，验 OCR 管线与降级 |
| 华东机电-补充.txt | 文本 | 同一供应商第二次报价（同供应商多文件合并） |

同时每份样本配 `expected/*.json`：人工（即本方案作者设定）核对的正确提取结果与预期标色。

**样本生成脚本** `tests/fixtures/generate.py` 也作为交付物：用 reportlab/openpyxl/Pillow 程序化生成上述文件，保证可复现、可扩充。

---

## 10. 风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| 真实报价单格式远超虚拟样本覆盖范围 | 高 | 样本生成脚本可扩充；设计上"定位失败→整文送 LLM→标黄"始终有兜底；上线后持续收集真实格式补进样本集 |
| LLM 幻觉编造价格 | 中 | H1 溯源硬校验 + temperature=0 + prompt 禁令；金额类字段错且未标黄在验收中计为事故 |
| 物料对齐错误合并 | 中 | 保守策略：LLM 匹配置信度 low 一律不合并；比价页相邻放置提示人工 |
| 扫描件/拍照件质量差 | 高 | 接受低准确率但必须标黄；预留视觉大模型备选通道（settings.yml 切换） |
| 虚拟样本与真实分布偏差导致"测试全过、实战翻车" | 中 | 这是无真实数据的固有代价。对策：MVP 完成后第一优先级是拿给行业内联系人试用，用真实报价单回填样本集 |

---

## 11. 后续路线（不在本次开发范围）

- v1.1：真实样本回填、更多格式模板
- v2.0：历史报价数据库（SQLite），支持"该物料上次采购价"对比
- v2.5：增值税/运费/汇率的总成本计算模式
- v3.0：极简本地网页（拖文件夹 → 下载 Excel），给非技术同事用
- 远期：与"图纸 BOM 提取工具"合并为同一代码库（两者共享 LLM 客户端、校验框架、导出框架），形成制造业文档结构化工具集
